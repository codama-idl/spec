/**
 * Output-target conventions for the generated docs tree.
 * The docs are emitted as GitHub-flavoured markdown, browsable directly on GitHub.
 */

/** File extension for emitted pages and for the relative links between them. */
export const PAGE_EXTENSION = '.md';

/** Landing-page basename - GitHub renders a folder's `README.md` when browsing the folder. */
export const INDEX_FILE_NAME = 'README';
