/**
 * GENERATED FILE - DO NOT EDIT.
 *
 * Kept in sync with the `version` field of `package.json` by `scripts/sync-spec-version.ts`,
 * which the release flow runs right after `changeset version` so the spec version lands in
 * the same commit as the package bump. See "Versioning" in CONTRIBUTING.md.
 */

/** The version string of the spec - always equal to the package version. */
export const SPEC_VERSION = '2.0.0-rc.0';
