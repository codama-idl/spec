{
    "name": "@codama-internal/release",
    "version": "0.0.0",
    "private": true,
    "description": "Polyglot release tooling for the Codama spec repo",
    "type": "module",
    "scripts": {
        "lint": "eslint . && prettier --check .",
        "lint:fix": "eslint --fix . && prettier --write .",
        "test": "vitest run",
        "test:types": "tsc --noEmit"
    },
    "bin": {
        "codama-release": "src/index.ts"
    },
    "dependencies": {
        "semver": "^7.7.4",
        "smol-toml": "^1.6.1",
        "yaml": "^2.8.4"
    },
    "devDependencies": {
        "@types/node": "^25",
        "@types/semver": "^7.7.1",
        "tsx": "^4.21.0",
        "typescript": "^6.0.3",
        "vitest": "^4.1.5"
    }
}
