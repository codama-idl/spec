name: Main

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: main-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  # Node 24 is required for npm trusted publishing (OIDC) in the release job;
  # see https://docs.npmjs.com/trusted-publishers (npm CLI ≥11.5.1, Node ≥22.14.0).
  # The js and rust jobs don't care about the Node version so we keep them all
  # aligned for simplicity.
  NODE_VERSION: 24
  PNPM_VERSION: 10.33.3

jobs:
  js:
    name: JavaScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm build

      - run: pnpm test

      - name: Verify generated artifacts are up to date
        run: |
          pnpm generate
          if ! git diff --quiet -- spec-v1.json js/node-types/src/generated; then
            echo '::error::Generated artifacts are stale. Re-run `pnpm generate` and commit the result.'
            git --no-pager diff -- spec-v1.json js/node-types/src/generated
            exit 1
          fi

  rust:
    name: Rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Read pinned Rust toolchain
        id: toolchain
        run: |
          channel=$(awk -F\" '/channel/ {print $2}' rust-toolchain.toml)
          echo "channel=$channel" >> "$GITHUB_OUTPUT"

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - run: cargo fmt --all -- --check

      - run: cargo clippy --workspace --all-targets -- -D warnings

      - run: cargo build --workspace --all-targets

      - run: cargo test --workspace

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [js, rust]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      # Note: no `registry-url` here. Setting it makes setup-node write
      # `_authToken=${NODE_AUTH_TOKEN}` into a temp .npmrc and point pnpm at
      # it. Without an NPM_TOKEN secret (we don't have one — we use OIDC) the
      # placeholder `XXXXX-XXXXX-XXXXX-XXXXX` lands in that .npmrc and pnpm
      # tries to authenticate with it, shadowing the OIDC token exchange.
      # Omitting `registry-url` leaves pnpm to read the registry from its own
      # config (defaulting to https://registry.npmjs.org) and lets its
      # built-in OIDC code path provide auth.
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Build before publishing so the dist artifacts are present in the
      # workspace when `changeset publish` runs.
      - run: pnpm build

      - name: Open Version Packages PR or publish
        uses: changesets/action@v1
        with:
          version: pnpm changeset version
          publish: pnpm changeset publish
          commit: 'release: version packages'
          title: 'release: version packages'
        env:
          # No NPM_TOKEN: this repo uses npm trusted publishing (OIDC).
          # `id-token: write` above lets pnpm exchange a GitHub OIDC token
          # for a short-lived publish token at `pnpm publish` time.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
